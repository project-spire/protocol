syntax = "proto3";

package spire.protocol;

enum ProtocolId {
  // auth
  Login = 0;
  LoginResult = 1;

  // net
  Ping = 1000;
  Pong = 1001;

  // play
  MovementCommand = 2000;
  MovementSync = 2001;
}
