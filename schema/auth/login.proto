syntax = "proto3";

import "uuid.proto";

package spire.protocol.auth;

message Login {
  enum Kind {
    Enter = 0;
    Transfer = 1;
  }

  Kind kind = 1;
  string token = 2;
  Uuid character_id = 3;
}
