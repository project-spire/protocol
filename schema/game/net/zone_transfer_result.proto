syntax = "proto3";

package spire.protocol.game.net;

// Client <- Server
message ZoneTransferResult {
    enum Result {
        Ok = 0;
    }

    Result result = 1;
    int64 zone_id = 2;
    uint32 zone_data_id = 3;
}
