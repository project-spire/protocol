syntax = "proto3";

package spire.protocol.game.net;

// Client <- Server
message ZoneTransferReadyResult {
    enum Result {
        Ok = 0;
        Standby = 1;
    }

    Result result = 1;
    int64 zone_id = 2;
}
