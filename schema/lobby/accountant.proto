syntax = "proto3";

import "uuid.proto";

package spire.protocol.lobby;

service Accountant {
    rpc GetDevAccount (DevAccountRequest) returns (AccountResponse);
}

message AccountResponse {
    Uuid id = 1;
}

message DevAccountRequest {
    string dev_id = 1;
}

